'use strict';

// const Router = require('../../../../models/vacancyModel').Router;

class RoutersController {

    async getRouters(ctx) {
        let params = ctx.params;

        // return await Routers.find(params)
        //     .select({

        //     });
    }

}

module.exports = new RoutersController();
