const mount = require('koa-mount');

module.exports = function (server) {

    // const userManagerMiddleware = require('./user-manager.middleware');
    // server.use(mount('/v2', userManagerMiddleware));

};
